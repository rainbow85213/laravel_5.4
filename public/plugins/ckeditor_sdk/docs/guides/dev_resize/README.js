Ext.data.JsonP.dev_resize({"guide":"<!--\nCopyright (c) 2003-2017, CKSource - Frederico Knabben. All rights reserved.\nFor licensing, see LICENSE.md.\n-->\n\n\n<h1 id='dev_resize-section-editor-resizing-customization'>Editor Resizing Customization</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_resize-section-disabling-editor-resizing'>Disabling Editor Resizing</a></li>\n<li>\n<a href='#!/guide/dev_resize-section-changing-editor-size-on-the-fly'>Changing Editor Size on the Fly</a></li>\n<li>\n<a href='#!/guide/dev_resize-section-limiting-the-width-and-height-for-ckeditor-resizing'>Limiting the Width and Height for CKEditor Resizing</a></li>\n<li>\n<a href='#!/guide/dev_resize-section-limiting-the-resizing-directions'>Limiting the Resizing Directions</a></li>\n<li>\n<a href='#!/guide/dev_resize-section-editor-resizing-customization-demo'>Editor Resizing Customization Demo</a></li>\n<li>\n<a href='#!/guide/dev_resize-section-related-features'>Related Features</a></li></ol>\n</div>\n\n<div class=\"requirements\">\n    <p>\n        This feature is provided through the <a href=\"http://ckeditor.com/addon/resize\">Editor Resize</a> plugin that is included in the Standard and Full presets available from the official CKEditor <a href=\"http://ckeditor.com/download\">Download</a> site. You can also <a href=\"#!/guide/dev_plugins\">add it to your custom build</a> with <a href=\"http://ckeditor.com/builder\">CKBuilder</a>.\n    </p>\n    <p>\n        Width and height manipulation is only supported for <a href=\"#!/guide/dev_framed\">classic editor</a> and does not work in <a href=\"#!/guide/dev_inline\">inline editor</a>.\n    </p>\n</div>\n\n\n<p>When the Editor Resize plugin is enabled, it adds the resize handle (<span title=\"Resize\">◢</span>) at the bottom of the editor user interface and allows users to manually resize the editor window to desired dimensions.</p>\n\n<p><p><img src=\"guides/dev_resize/resize_01.png\" alt=\"The editor resize handle\" width=\"711\" height=\"270\"></p></p>\n\n<p>The resize feature is highly customizable. It is possible to define the resizing direction or maximum and minimum editor size after the resize. Additionally, this plugin provides the <a href=\"#!/api/CKEDITOR.editor-method-resize\" rel=\"CKEDITOR.editor-method-resize\" class=\"docClass\">CKEDITOR.editor.resize</a> method which allows for resizing the editor on the fly.</p>\n\n<h2 id='dev_resize-section-disabling-editor-resizing'>Disabling Editor Resizing</h2>\n\n<p>To prevent the editor from being resized you can use the <a href=\"#!/api/CKEDITOR.config-cfg-removePlugins\" rel=\"CKEDITOR.config-cfg-removePlugins\" class=\"docClass\">CKEDITOR.config.removePlugins</a> setting to remove the Editor Resize (<code>resize</code>) plugin.</p>\n\n<pre><code>config.removePlugins = 'resize';\n</code></pre>\n\n<p>You can also disable this feature by setting the <a href=\"#!/api/CKEDITOR.config-cfg-resize_enabled\" rel=\"CKEDITOR.config-cfg-resize_enabled\" class=\"docClass\">CKEDITOR.config.resize_enabled</a> configuration option to <code>false</code>.</p>\n\n<pre><code>config.resize_enabled = false;\n</code></pre>\n\n<h2 id='dev_resize-section-changing-editor-size-on-the-fly'>Changing Editor Size on the Fly</h2>\n\n<p>Besides defining a <a href=\"#!/guide/dev_size\">default size</a> of the editor window you can also change the size of a CKEditor instance on the fly.</p>\n\n<p>To achieve this, use the <a href=\"#!/api/CKEDITOR.editor-method-resize\" rel=\"CKEDITOR.editor-method-resize\" class=\"docClass\">editor.resize()</a> method to define the dimensions of the editor interface, assigning the window a width and height value in pixels or CSS-accepted units.</p>\n\n<pre><code>// Set editor width to 100% and height to 350px.\neditor.resize( '100%', '350' );\n</code></pre>\n\n<p>While setting the height value, use the <code>isContentHeight</code> parameter to decide whether the value applies to the entire editor interface or just the editing area.</p>\n\n<pre><code>// The height value now applies to the editing area.\neditor.resize( '100%', '350', true );\n</code></pre>\n\n<h2 id='dev_resize-section-limiting-the-width-and-height-for-ckeditor-resizing'>Limiting the Width and Height for CKEditor Resizing</h2>\n\n<p>It is also possible to define the minimum and maximum dimensions after resizing to prevent the editor window from becoming too small or too big to handle.</p>\n\n<p>To define the minimum editor dimensions after resizing, specify the <a href=\"#!/api/CKEDITOR.config-cfg-resize_minWidth\" rel=\"CKEDITOR.config-cfg-resize_minWidth\" class=\"docClass\">CKEDITOR.config.resize_minWidth</a> and <a href=\"#!/api/CKEDITOR.config-cfg-resize_minHeight\" rel=\"CKEDITOR.config-cfg-resize_minHeight\" class=\"docClass\">CKEDITOR.config.resize_minHeight</a> values in pixels.</p>\n\n<pre><code>config.resize_minWidth = 300;\nconfig.resize_minHeight = 300;\n</code></pre>\n\n<p>To define the maximum editor dimensions after resizing, specify the <a href=\"#!/api/CKEDITOR.config-cfg-resize_maxWidth\" rel=\"CKEDITOR.config-cfg-resize_maxWidth\" class=\"docClass\">CKEDITOR.config.resize_maxWidth</a> and <a href=\"#!/api/CKEDITOR.config-cfg-resize_maxHeight\" rel=\"CKEDITOR.config-cfg-resize_maxHeight\" class=\"docClass\">CKEDITOR.config.resize_maxHeight</a> values in pixels.</p>\n\n<pre><code>config.resize_maxWidth = 800;\nconfig.resize_maxHeight = 600;\n</code></pre>\n\n<h2 id='dev_resize-section-limiting-the-resizing-directions'>Limiting the Resizing Directions</h2>\n\n<p>Additionally, you can define the resizing directions in order to have more control over the resulting editor appearance.</p>\n\n<p>By default CKEditor is allowed to only resize vertically. This is achieved thanks to setting the <a href=\"#!/api/CKEDITOR.config-cfg-resize_dir\" rel=\"CKEDITOR.config-cfg-resize_dir\" class=\"docClass\">CKEDITOR.config.resize_dir</a> configuration option to <code>'vertical'</code></p>\n\n<pre><code>config.resize_dir = 'vertical';\n</code></pre>\n\n<p>If you set the <a href=\"#!/api/CKEDITOR.config-cfg-resize_dir\" rel=\"CKEDITOR.config-cfg-resize_dir\" class=\"docClass\">CKEDITOR.config.resize_dir</a> configuration option to <code>'horizontal'</code>, CKEditor window will only be resizable in horizontal dimension.</p>\n\n<pre><code>config.resize_dir = 'horizontal';\n</code></pre>\n\n<p>If you want to allow both vertical and horizontal resizing, you need to set the <a href=\"#!/api/CKEDITOR.config-cfg-resize_dir\" rel=\"CKEDITOR.config-cfg-resize_dir\" class=\"docClass\">CKEDITOR.config.resize_dir</a> configuration option to <code>'both'</code>.</p>\n\n<pre><code>config.resize_dir = 'both';\n</code></pre>\n\n<h2 id='dev_resize-section-editor-resizing-customization-demo'>Editor Resizing Customization Demo</h2>\n\n<p>See the <a href=\"../samples/resize.html\">working \"Editor Resizing Customization\" sample</a> that showcases an editor instance with modified resizing settings.</p>\n\n<h2 id='dev_resize-section-related-features'>Related Features</h2>\n\n<p>Refer to the following resources for more information about editor resizing:</p>\n\n<ul>\n<li>The <a href=\"#!/guide/dev_size\">Setting Editor Size</a> article explains how to set the editor width and height.</li>\n<li>The <a href=\"#!/guide/dev_autogrow\">Auto Grow</a> feature allows the editor to automatically expand and shrink vertically depending on the amount and size of content entered in its editing area.</li>\n</ul>\n\n","title":"Resizing Customization","meta_description":"Editor resizing customization and on the fly resizing.","meta_keywords":"ckeditor, editor, wysiwyg, size, resize, resizing, dimension, height, width, configure, configuration, setup, settings, options, customization, customize, customise, customisation, config, modification, modify, change"});