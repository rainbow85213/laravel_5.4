Ext.data.JsonP.dev_sharedspace({"guide":"<!--\nCopyright (c) 2003-2017, CKSource - Frederico Knabben. All rights reserved.\nFor licensing, see LICENSE.md.\n-->\n\n\n<h1 id='dev_sharedspace-section-shared-toolbar-and-bottom-bar'>Shared Toolbar and Bottom Bar</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_sharedspace-section-customization'>Customization</a></li>\n<li>\n<a href='#!/guide/dev_sharedspace-section-tips-and-tricks'>Tips and Tricks</a></li>\n<li>\n<a href='#!/guide/dev_sharedspace-section-shared-toolbar-and-bottom-bar-demo'>Shared Toolbar and Bottom Bar Demo</a></li>\n<li>\n<a href='#!/guide/dev_sharedspace-section-related-features'>Related Features</a></li></ol>\n</div>\n\n<p class=\"requirements\">\n    This feature is provided through an optional plugin that is not included in the CKEditor presets available from the <a href=\"http://ckeditor.com/download\">Download</a> site and <a href=\"#!/guide/dev_plugins\">needs to be added to your custom build</a> with <a href=\"http://ckeditor.com/builder\">CKBuilder</a>.\n</p>\n\n\n<p>An optional CKEditor plugin called <a href=\"http://ckeditor.com/addon/sharedspace\">Shared Space</a> lets you put the editor toolbar and bottom bar in selected page elements detached from the editing areas and then share these UI elements among numerous editor instances.</p>\n\n<p>This feature can be used for both <a href=\"#!/guide/dev_framed\">classic</a>, <code>iframe</code>-based editors, and <a href=\"#!/guide/dev_inline\">inline</a> editors. In case of inline editor instances this will ensure that the toolbar will always remain in one designated place on the page which may be beneficial is some usage scenarios.</p>\n\n<p>Please note that the UI elements can be shared between classic and inline editors placed on one page, too.</p>\n\n<h2 id='dev_sharedspace-section-customization'>Customization</h2>\n\n<p>This feature can be configured separately for each editor instance by setting the <a href=\"#!/api/CKEDITOR.config-cfg-sharedSpaces\" rel=\"CKEDITOR.config-cfg-sharedSpaces\" class=\"docClass\">CKEDITOR.config.sharedSpaces</a> option.</p>\n\n<p>This configuration option defines the IDs of the page elements that will store the editor UI elements. For example the following code will place the toolbar (the <code>top</code> part) in the element with an ID of <code>someElementId</code>, while the bottom bar (the <code>bottom</code> part) will be displayed in the element with an ID of <code>anotherId</code>.</p>\n\n<pre><code>config.sharedSpaces = {\n    top: 'someElementId',\n    bottom: 'anotherId'\n};\n</code></pre>\n\n<p>The image below presents this editor configuration for two classic editor instances.</p>\n\n<p><p><img src=\"guides/dev_sharedspace/sharedspace_01.png\" alt=\"\" width=\"925\" height=\"547\"></p></p>\n\n<p>If, on the other hand, you only want to share some UI elements and leave others to particular instances, you can use the settings like the following:</p>\n\n<pre><code>config.sharedSpaces = {\n    top: 'someElementId'\n};\n</code></pre>\n\n<p>An editor instance with such configuration will use the shared toolbar (the <code>top</code> element) while preserving its own bottom bar with the elements path, as shown in the image below with the same two classic editor instances.</p>\n\n<p><p><img src=\"guides/dev_sharedspace/sharedspace_02.png\" alt=\"\" width=\"922\" height=\"576\"></p></p>\n\n<p class=\"tip\">\n    Please note that you need to create the page elements that will store the toolbar and bottom bar by yourself and make sure you provide valid IDs to the editor configuration.\n</p>\n\n\n<h2 id='dev_sharedspace-section-tips-and-tricks'>Tips and Tricks</h2>\n\n<p>The Shared Space feature affects a crucial editor element &mdash; its user interface &mdash; so some fine-tuning of the editor configuration might be needed. Here are a few tips:</p>\n\n<ul>\n<li>The <a href=\"http://ckeditor.com/addon/resize\">Editor Resize</a> plugin should be disabled as it would not work in this context.</li>\n<li>Since CKEditor 4.5.5 the <a href=\"http://ckeditor.com/addon/maximize\">Maximize</a> plugin is disabled automatically in the shared space context.</li>\n</ul>\n\n\n<pre>\nconfig.removePlugins = 'resize';\n</pre>\n\n\n<h2 id='dev_sharedspace-section-shared-toolbar-and-bottom-bar-demo'>Shared Toolbar and Bottom Bar Demo</h2>\n\n<p>See the <a href=\"../samples/sharedspace.html\">working \"Shared Toolbar and Bottom Bar\" sample</a> that showcases how multiple editor instances can share the same toolbar and bottom bar.</p>\n\n<h2 id='dev_sharedspace-section-related-features'>Related Features</h2>\n\n<p>Refer to the following resources for more information about editor toolbar:</p>\n\n<ul>\n<li>The <a href=\"#!/guide/dev_toolbar\">Toolbar Configuration</a> article explains how to set up the editor toolbar using the toolbar configurator (CKEditor 4.5+).</li>\n<li>The <a href=\"#!/guide/dev_toolbarconcepts\">Understanding CKEditor Toolbar Concepts</a> article explains the basic concepts behind the editor toolbar.</li>\n<li>The <a href=\"#!/guide/dev_toolbarlocation\">Toolbar Location</a> feature lets you change the toolbar position in classic editor.</li>\n</ul>\n\n","title":"Shared UI","meta_description":"Sharing user interface elements among editor instances.","meta_keywords":"ckeditor, editor, wysiwyg, ui, interface, toolbar, blocks, sharing, common, shared, spaces, configure, configuration, setup, settings, options, customization, customize, customise, customisation, config, modification, modify, change"});