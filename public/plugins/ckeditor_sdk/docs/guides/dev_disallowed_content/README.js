Ext.data.JsonP.dev_disallowed_content({"guide":"<!--\nCopyright (c) 2003-2017, CKSource - Frederico Knabben. All rights reserved.\nFor licensing, see LICENSE.md.\n-->\n\n\n<h1 id='dev_disallowed_content-section-disallowed-content'>Disallowed Content</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/dev_disallowed_content-section-disallowed-content-rules'>Disallowed Content Rules</a></li>\n<li>\n<a href='#!/guide/dev_disallowed_content-section-how-to-allow-everything-except...'>How to Allow Everything Except...</a></li>\n<li>\n<a href='#!/guide/dev_disallowed_content-section-further-reading'>Further Reading</a></li></ol>\n</div>\n\n<p class=\"requirements\">\n    The Disallowed Content feature was introduced in <strong>CKEditor 4.4</strong> as a part of the <a href=\"#!/guide/dev_advanced_content_filter\">Advanced Content Filter</a> system which was introduced in <strong>CKEditor 4.1</strong>.\n</p>\n\n\n<p>The Disallowed Content feature complements the existing Allowed Content feature in that it lets you explicitly blacklist elements that you do not want to have in your CKEditor content. You can use it to limit the features that would otherwise be available in the fully automatic Advanced Content Filter mode.</p>\n\n<p>Disallowed content can be defined in the <a href=\"#!/api/CKEDITOR.config-cfg-disallowedContent\" rel=\"CKEDITOR.config-cfg-disallowedContent\" class=\"docClass\">CKEDITOR.config.disallowedContent</a> setting or may be dynamically added with the <a href=\"#!/api/CKEDITOR.filter-method-disallow\" rel=\"CKEDITOR.filter-method-disallow\" class=\"docClass\">CKEDITOR.filter.disallow</a> method. It has a higher priority than content allowed automatically or by the <a href=\"#!/api/CKEDITOR.config-cfg-allowedContent\" rel=\"CKEDITOR.config-cfg-allowedContent\" class=\"docClass\">CKEDITOR.config.allowedContent</a> setting, hence it is possible to correct automatic settings or achieve more complex results like \"allow all attributes except those starting with <code>on*</code>\".</p>\n\n<p class=\"tip\">\n    It is not possible to disallow content when the Advanced Content Filter is disabled by setting <a href=\"#!/api/CKEDITOR.config-cfg-allowedContent\" rel=\"CKEDITOR.config-cfg-allowedContent\" class=\"docClass\">CKEDITOR.config.allowedContent</a> to <code>true</code>. A similar solution can be achieved by allowing all HTML elements.\n</p>\n\n\n<h2 id='dev_disallowed_content-section-disallowed-content-rules'>Disallowed Content Rules</h2>\n\n<p>Disallowed content rules are very similar to the <a href=\"#!/guide/dev_allowed_content_rules\">allowed content rules</a>. They can be specified in two formats (<a href=\"#!/guide/dev_allowed_content_rules-section-string-format\">string</a> and <a href=\"#!/guide/dev_allowed_content_rules-section-object-format\">object</a>), however, it is not possible to specify required properties (which simply would not make any sense in this case).</p>\n\n<p>When only element names are defined, a rule disallows entire elements (and thus these elements will be removed). When a rule also contains property names, only these properties (attributes, styles, classes) will be removed, not elements (unless some required property was removed).</p>\n\n<p>Best to see it in examples.</p>\n\n<ol>\n<li><p>Disallowing entire elements.</p>\n\n<pre><code> config.allowedContent = 'h1 h2 h3 p';\n config.disallowedContent = 'h2 h3';\n // Input:       &lt;h1&gt;Foo&lt;/h1&gt;&lt;h2&gt;Bar&lt;/h2&gt;&lt;h3&gt;Bom&lt;/h3&gt;\n // Filtered:    &lt;h1&gt;Foo&lt;/h1&gt;&lt;p&gt;Bar&lt;/p&gt;&lt;p&gt;Bom&lt;/p&gt;\n</code></pre></li>\n<li><p>Disallowing attributes, classes, and styles.</p>\n\n<pre><code> config.allowedContent = 'p[*]{*}(foo,bar)';\n config.disallowedContent = 'p[on*](foo)';\n // Input:       &lt;p&gt;Foo&lt;/p&gt;&lt;p onclick=\"...\" data-foo=\"1\" class=\"foo bar\"&gt;Bar&lt;/p&gt;\n // Filtered:    &lt;p&gt;Foo&lt;/p&gt;&lt;p data-foo=\"1\" class=\"bar\"&gt;Bar&lt;/p&gt;\n</code></pre></li>\n<li><p>Disallowing a required property.</p>\n\n<pre><code> config.allowedContent = 'p; img[!src,alt]';\n config.disallowedContent = 'img[src]';\n // Input:       &lt;p&gt;&lt;img src=\"...\" alt=\"...\" /&gt;&lt;/p&gt;\n // Filtered:    &lt;p&gt;&lt;/p&gt;\n</code></pre></li>\n<li><p>Disallowing properties on all elements.</p>\n\n<pre><code> config.allowedContent = 'p em{*}';\n config.disallowedContent = '*{font*}';\n // Input:       &lt;p style=\"color: red; font-size: 12px\"&gt;&lt;em style=\"font: 'Arial'\"&gt;Foo&lt;/em&gt;&lt;/p&gt;\n // Filtered:    &lt;p style=\"color: red\"&gt;&lt;em&gt;Foo&lt;/em&gt;&lt;/p&gt;\n</code></pre></li>\n<li><p>Tweaking automatically allowed content.</p>\n\n<pre><code> // Enabled plugins: image and table.\n config.disallowedContent = 'img{border*,margin*}; table[border]{*}';\n</code></pre>\n\n<p> With this code implemented, open the Image Properties dialog window and see that the Border, HSpace, and VSpace fields were hidden. You can also open the Table dialog window and see that the Border, Width, and Height fields were hidden, too.</p></li>\n</ol>\n\n\n<h2 id='dev_disallowed_content-section-how-to-allow-everything-except...'>How to Allow Everything Except...</h2>\n\n<p>A popular requirement is to allow all HTML features except a few specific ones. In this case setting <a href=\"#!/api/CKEDITOR.config-cfg-allowedContent\" rel=\"CKEDITOR.config-cfg-allowedContent\" class=\"docClass\">CKEDITOR.config.allowedContent</a> to <code>true</code> is not a solution, because this completely disables the <a href=\"#!/guide/dev_advanced_content_filter\">Advanced Content Filter</a>, so <a href=\"#!/api/CKEDITOR.config-cfg-disallowedContent\" rel=\"CKEDITOR.config-cfg-disallowedContent\" class=\"docClass\">CKEDITOR.config.disallowedContent</a> will not work either.</p>\n\n<p>Therefore the only solution is to write an allowed content rule allowing all elements. Fortunately, this can be done easily by using the <a href=\"#!/guide/dev_allowed_content_rules-section-object-format\">object format of allowed content rules</a> and the <a href=\"#!/api/CKEDITOR.dtd\" rel=\"CKEDITOR.dtd\" class=\"docClass\">CKEDITOR.dtd</a> object.</p>\n\n<pre><code>config.allowedContent = {\n    $1: {\n        // Use the ability to specify elements as an object.\n        elements: <a href=\"#!/api/CKEDITOR.dtd\" rel=\"CKEDITOR.dtd\" class=\"docClass\">CKEDITOR.dtd</a>,\n        attributes: true,\n        styles: true,\n        classes: true\n    }\n};\nconfig.disallowedContent = 'script; *[on*]';\n</code></pre>\n\n<p>The above code sample will allow everything except for the <code>&lt;script&gt;</code> elements and attributes starting from <code>'on'</code>.</p>\n\n<h2 id='dev_disallowed_content-section-further-reading'>Further Reading</h2>\n\n<p>Refer to the following resources for more information about content filtering:</p>\n\n<ul>\n<li>The <a href=\"#!/guide/dev_acf\">Content Filtering (ACF)</a> article explains some ACF use cases and the rationale behind this feature.</li>\n<li>The <a href=\"#!/guide/dev_advanced_content_filter\">Advanced Content Filer</a> article contains more in-depth technical details about ACF.</li>\n<li>The <a href=\"#!/guide/dev_allowed_content_rules\">Allowed Content Rules</a> article explains the allowed and disallowed content rules format.</li>\n<li>The <a href=\"#!/guide/plugin_sdk_integration_with_acf\">Integrating Plugins with Advanced Content Filter</a> article explains how to adjust custom plugins to properly implement content filtering.</li>\n</ul>\n\n","title":"Disallowed Content","meta_description":"Information about Disallowed Content for Advanced Content Filter.","meta_keywords":"ckeditor, editor, wysiwyg, acf, filter, content, filtering, disallowed, disallow, rule, rules"});